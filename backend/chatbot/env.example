# MindEase Chatbot Configuration
# Copy this file to .env (or set environment variables) and update with your values

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mentalhealthdb
DB_USER=postgres
DB_PASSWORD=pakistan

# Model Paths (optional - defaults to project root)
# DEBERTA_MODEL_PATH=/path/to/deberta_best
# DATASET_PATH=/path/to/dataset/MentalChat16K.csv

# Ollama Configuration
OLLAMA_MODEL=llama3.1:8b-instruct

