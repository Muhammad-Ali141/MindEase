# Text-to-Speech Module Dependencies
# Prefer installing from backend/requirements.txt which pins versions for TTS + STT + ML.
# Or install here: pip install -r requirements.txt
#
# IMPORTANT: Coqui TTS requires Python >= 3.9 and < 3.12
# If you have Python 3.12+, use Python 3.10 or 3.11, or see README.md for alternatives.
#
# Versions aligned with backend/requirements.txt (Coqui TTS 0.22.0, XTTS):
# - TTS==0.22.0, torch==2.3.1, torchvision==0.18.1, numpy==1.26.4
# - pandas>=1.4,<2.0 (required by TTS 0.22.0)

# Core TTS library (Coqui TTS 0.22.0 for XTTS)
TTS==0.22.0

# PyTorch for model inference (match backend for CUDA/cuDNN)
torch>=2.0.0
torchaudio>=2.0.0

# Audio processing
soundfile==0.12.1
sounddevice>=0.4.6
pydub>=0.25.1

# Numerical processing (numpy 1.x for binary compatibility with TTS/sklearn)
numpy>=1.24.0,<2.0
scipy>=1.11.2

# Utilities
tqdm>=4.65.0

