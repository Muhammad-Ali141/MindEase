# ============================
# MindEase Backend Requirements (Fixed)
# ============================
# Install: from repo root with venv active: pip install -r backend/requirements.txt
# If you see "Invalid distribution ~orch" or OSError during install, remove any
# ~orch or ~cikit_learn folders in venv/Lib/site-packages then retry.

# Django
Django==5.1.3
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.0
django-cors-headers==4.3.1

# Database
psycopg2-binary==2.9.9

# Environment
python-dotenv==1.0.1

# ============================
# ML / NLP — conflict-free
# ============================

# Transformers 4.35.2 required for Coqui TTS (XTTS): BeamSearchScorer + GPT2InferenceModel.generate
# (4.33.0 lacks generate fix; >4.35.2 breaks XTTS)
transformers==4.35.2

# Sentence Transformers 2.2.2 compatible with transformers 4.35.2
sentence-transformers==2.2.2

torch==2.3.1
torchvision==0.18.1

# cuDNN 9 for CUDA 12 (fixes cudnn_ops64_9.dll on Windows; RTX 4070)
nvidia-cudnn-cu12>=9.9.0

numpy==1.26.4
scipy==1.11.4
scikit-learn==1.4.2
# TTS 0.22.0 requires pandas>=1.4,<2.0; keep compatible with numpy 1.x for sklearn
pandas>=1.4.0,<2.0.0

# ============================
# TTS — Coqui TTS 0.22.0 compatible
# ============================
TTS==0.22.0
soundfile==0.12.1
sounddevice==0.5.3
pydub==0.25.1

# ============================
# Speech-to-Text (faster-whisper 1.2.1 — no live transcription; record then transcribe)
# ============================
faster-whisper==1.2.1
resampy==0.4.2

# ============================
# Utilities
# ============================
tqdm==4.66.1
requests==2.31.0
PyYAML==6.0.1
pillow==10.4.0
nltk==3.9.1

# ============================
# Supporting Libraries — pinned for stability (transformers 4.35.2 + TTS + STT)
# ============================
# transformers 4.35.2 requires huggingface-hub>=0.16.4,<1.0 and tokenizers>=0.14,<0.19
huggingface-hub>=0.16.4,<1.0
safetensors==0.4.5
tokenizers>=0.14.0,<0.19
sentencepiece==0.2.0
regex==2023.10.3
packaging==23.2
typing_extensions==4.8.0
filelock==3.12.2
fsspec==2023.6.0
Jinja2==3.1.3
MarkupSafe==2.1.3
certifi==2023.11.17
charset-normalizer==3.3.0
idna==3.4
urllib3==2.1.0
click==8.1.7
colorama==0.4.6
joblib==1.3.2
threadpoolctl==3.2.0
networkx==2.8.8
mpmath==1.3.0
sympy==1.12
cffi==1.16.0
pycparser==2.21